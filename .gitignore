node_modules/

# 빌드 산출물 — apps/web/dist 는 Vercel이 .gitignore로 배포물에서 제외하므로 여기 적지 않음(실수로 커밋하지 말 것).
apps/api/dist/

# 로컬 BFF SQLite (개발 데이터; 커밋 금지)
/data/
.env
.env.*
!.env.example
*.local
.DS_Store
Thumbs.db

# Stitch export (large HTML/PNG reference; not required for build)
/stitch_gesti_n_de_redes */
**/tsconfig.tsbuildinfo
apps/web/test-results/
playwright-report/

# Vercel CLI: 링크 메타만 공유 (README 등은 제외)
.vercel/*
!.vercel/project.json
